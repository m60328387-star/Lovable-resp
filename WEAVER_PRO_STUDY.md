# دراسة شاملة: رفع Weaver إلى أقصى درجة احترافية

تاريخ الدراسة: 2026-08-11 · النسخة الحالية: 38k سطر كود، 5 حاويات إنتاج على كونتابو، ~50 أداة وكيل.

---

## 1. أين يقف Weaver اليوم

| المحور | الحالة | التقييم |
|---|---|---|
| دورة العمل (Intake→Deploy) | مكتملة ومطبّقة | 9/10 |
| أدوات الوكيل | ~50 أداة (ملفات، تنفيذ، متصفح، نشر، معرفة) | 8/10 |
| البنية التحتية | خادم واحد + Docker + Nginx + Postgres + نسخ احتياطي | 6/10 |
| جودة التصميم المُنتَج | عقد تصميم إلزامي + بوابة جودة ≥85 | 7/10 |
| الإصلاح الذاتي | Staging + Smoke + Diff + Rollback | 8/10 |
| المراقبة والرصد | /status /trace /monitor /health | 7/10 |
| الاختبارات الآلية للمنصة نفسها | **غير موجودة** | 1/10 |
| الأمان والحدود | رمز واحد + قفل محاولات؛ لا حدود معدل شاملة | 5/10 |
| الذاكرة طويلة الأمد | نصية فقط (لا بحث دلالي حقيقي) | 4/10 |
| التخزين الكائني | لا يوجد (كل شيء على قرص الخادم) | 3/10 |

**الخلاصة:** المنصة قوية وظيفياً، وضعفها الحقيقي في *الموثوقية الهندسية* (اختبارات، حدود، تخزين، ذاكرة دلالية) لا في المزايا.

---

## 2. الفجوات الحرجة (مرتبة حسب الأثر)

### ف1 — لا توجد شبكة أمان اختبارية للمنصة
لا `vitest`، لا CI، لا اختبارات وحدة أو تكامل. كل تعديل ذاتي يعتمد على lint + smoke سطحي.
**الخطر:** الوكيل يكسر نفسه ولا يكتشف ذلك إلا في الإنتاج.

### ف2 — ملفات عملاقة تكسر التعديل الجراحي
`workspace.ts` = 2481 سطر، `platform.tsx` = 1413، `project-panel.tsx` = 1378.
**الخطر:** كل تعديل يستهلك سياقاً هائلاً ويزيد احتمال الكتابة فوق كود سليم.

### ف3 — الذاكرة ليست دلالية
`knowledge_entries` بلا `tsvector` ولا embeddings ⇒ الاسترجاع ضعيف ⇒ الوكيل يعيد اختراع نفس الحلول.

### ف4 — لا تخزين كائني
الأرشيفات، لقطات المتصفح، مخرجات البناء كلها على قرص الخادم ⇒ نمو غير محدود وخطر فقدان.

### ف5 — الطابور غير دائم
لا `heavyQueue` فعلي في `runtime.server.ts`؛ إعادة تشغيل الحاوية = فقدان المهام الجارية.

### ف6 — حدود المعدل جزئية
موجودة في `/api/chat` فقط. مسارات `/api/public/*` و`/s/*` مكشوفة.

### ف7 — لا مقاييس زمنية (metrics)
لا p95 لزمن الأداة، لا معدل فشل لكل أداة، لا تكلفة لكل مشروع بشكل تحليلي.

---

## 3. خطة الترقية — 5 موجات

### الموجة A — شبكة الأمان (الأولوية القصوى)
1. `vitest` + `bun run test` في package.json.
2. اختبارات وحدة لـ: `design/metrics`, `context-compaction`, `diff`, `verify.server`, `build-state`, `local-supabase`.
3. اختبارات تكامل لـ: تسلسل الأدوات (starter_kit→direction→brand_kit→files) وبوابة عقد التصميم.
4. ربط `bun run lint && bun run test` كبوابة إلزامية داخل `platform.server.ts` قبل أي `deploy`.
5. اختبار دخان E2E حقيقي بـ Playwright على staging: تسجيل دخول → إنشاء مشروع → معاينة تعمل.

**الأثر:** يمنع 80% من الأعطال الذاتية.

### الموجة B — تفكيك الوحدات العملاقة
- `workspace.ts` → `tools/fs.ts`, `tools/exec.ts`, `tools/design-gate.ts`, `tools/preview.ts`.
- `platform.tsx` → مكوّنات: `StageCard`, `DiffCard`, `SmokeCard`, `RollbackCard`, `DeployLog`.
- `project-panel.tsx` → `PreviewTab`, `FilesTab`, `TerminalTab`, `AuditTab`.
- قاعدة صارمة: أي ملف > 400 سطر يُرفض في بوابة التحقق.

### الموجة C — ذاكرة دلالية حقيقية
1. عمود `tsvector` + فهرس GIN على `knowledge_entries` (عربي + إنجليزي) — حل فوري بلا تكلفة.
2. لاحقاً: `pgvector` + embeddings عبر OpenRouter، مع `recall_knowledge` هجين (BM25 + cosine).
3. ترقية تلقائية: عند نجاح مشروع، يُستخرج «النمط» (لا الكود الخام) ويُخزَّن كقالب قابل لإعادة الاستخدام.

### الموجة D — صلابة البنية التحتية على خادم واحد
1. طابور دائم في Postgres (`agent_jobs` موجود — يُوسَّع ليشمل مهام Runtime الثقيلة) مع استئناف بعد إعادة التشغيل.
2. حدود معدل موحّدة عبر middleware واحد على كل `/api/public/*` و`/s/*`.
3. تدوير تلقائي للسجلات وحذف مساحات العمل الخاملة > 48 ساعة.
4. تخزين كائني: MinIO كحاوية سادسة (أو Cloudflare R2 مجاناً) للأرشيفات واللقطات.
5. مراقبة الموارد: تنبيه تلقائي عبر بوت تيليجرام عند RAM > 85% أو قرص > 80%.

### الموجة E — القياس والاحترافية التشغيلية
1. جدول `tool_metrics`: اسم الأداة، المدة، النجاح/الفشل، المشروع.
2. لوحة `/metrics`: p50/p95 لكل أداة، أعلى 10 أدوات فشلاً، تكلفة/مشروع، متوسط زمن «من الطلب إلى معاينة تعمل».
3. تقرير أسبوعي تلقائي في تيليجرام.
4. SLO معلن داخلياً: نجاح بناء أول مرة ≥ 85%، زمن أول معاينة ≤ 4 دقائق.

---

## 4. ما الذي يرفع *جودة المواقع المُنتَجة* تحديداً

1. **مكتبة مراجع بصرية مصنّفة** — 30–40 نمطاً موصوفاً بدقة (تباعد، أوزان خطوط، إيقاع الشبكة) بدل الأوصاف العامة.
2. **بوابة بصرية إلزامية بلقطة شاشة** — الوكيل لا يعلن الإنجاز قبل لقطة Playwright + نقد آلي ≥ 85.
3. **فحص إمكانية الوصول والأداء** — Lighthouse مصغّر: تباين الألوان، حجم الحزمة، LCP.
4. **قوالب محتوى عربية حقيقية** — لا Lorem، بل نصوص تسويقية مولّدة بالسياق.
5. **قفل الرموز التصميمية** — منع أي لون/ظل خارج `brand/tokens.css` عبر فحص ثابت على CSS المكتوب.

---

## 5. الترتيب المقترح للتنفيذ

| # | العمل | الجهد | الأثر |
|---|---|---|---|
| 1 | Vitest + اختبارات أساسية + بوابة قبل النشر | متوسط | ★★★★★ |
| 2 | tsvector للمعرفة | صغير | ★★★★ |
| 3 | حدود معدل موحّدة + تنظيف تلقائي | صغير | ★★★★ |
| 4 | تفكيك workspace.ts و platform.tsx | متوسط | ★★★★ |
| 5 | جدول المقاييس + لوحة /metrics | متوسط | ★★★ |
| 6 | طابور دائم للمهام الثقيلة | متوسط | ★★★ |
| 7 | MinIO/R2 للتخزين الكائني | متوسط | ★★★ |
| 8 | مكتبة المراجع البصرية الموسّعة | كبير | ★★★★★ |
| 9 | pgvector + embeddings | كبير | ★★★ |

**التوصية:** ابدأ بالبنود 1–3 (يمكن إنجازها في جولة واحدة) لأنها تحوّل المنصة من «تعمل» إلى «موثوقة»، ثم البند 8 لأنه المحرّك الحقيقي لجودة المخرجات.

# تقرير Weaver الشامل — الفحص، الإصلاحات، وخارطة الوصول لمستوى Replit و Manus

تاريخ التقرير: 2026-08-11

---

## 1) نتيجة الفحص الشامل الحالي

| الفحص | النتيجة |
|---|---|
| TypeScript (`tsgo --noEmit`) | ✅ صفر أخطاء |
| ESLint (`--max-warnings=0`) | ✅ صفر أخطاء وصفر تحذيرات (بعد الإصلاح) |
| بناء الإنتاج (`vite build` + Nitro) | ✅ نجح — لا أخطاء بناء |
| فحص ثغرات الحزم (npm audit) | ✅ لا ثغرات عالية أو حرجة |
| اختبارات آلية (Vitest) | ⚠️ لا توجد ملفات اختبار بعد (فجوة رئيسية) |

### الإصلاحات المنفّذة في هذه الجولة
1. `src/components/agent/chat-window.tsx` — كان `parts` يُعاد إنشاؤه في كل رندر ويُستخدم كتبعية في `useEffect`، ما يسبّب **بثّ أحداث الطرفية بشكل متكرّر/حلقات رندر غير ضرورية**. تم لفّه بـ `useMemo`.
2. `eslint.config.js` — استثناء مقصود لقاعدة `react-refresh/only-export-components` لملفات shadcn UI و`app-shell`/`split-pane` التي تصدّر هوكات بجانب المكوّنات عمداً؛ الآن الـ lint نظيف تماماً ويصلح كبوابة CI صارمة.
3. تنسيق Prettier موحّد عبر المشروع.

---

## 2) الفجوات الحقيقية مقابل Replit و Manus

الفارق لم يعد في «ذكاء الوكيل» بل في **البنية التحتية والضمانات**. الترتيب حسب الأثر:

### أ. عزل التنفيذ (الأهم)
- **الوضع الآن**: كل مشاريع العملاء تعمل على نفس خادم Contabo داخل حاويات Docker مع حدود موارد + swap 4GB.
- **Replit/Manus**: كل جلسة داخل microVM معزولة (Firecracker/gVisor) — هروب من الحاوية لا يعني اختراق المنصة.
- **الأثر**: عند بيع المشاريع للعملاء، تنفيذ كود غير موثوق على نفس الخادم = خطر حقيقي.

### ب. الذاكرة الدلالية (Vector Memory)
- **الآن**: `knowledge_entries` نصية + بحث كلمات مفتاحية.
- **المطلوب**: pgvector + embeddings ليستدعي الوكيل «الحل الذي نجح سابقاً» بدقة دلالية لا لفظية. هذه أكبر رافعة لجودة/سرعة البناء المتكرر.

### ج. الاختبار البصري الإلزامي والقياس
- `browser_check` و`design_review` موجودان، لكن لا يوجد **حفظ لقطات مرجعية (visual regression baselines)** ولا مقاييس تراكمية (نسبة نجاح البناء، متوسط الجولات، تكلفة كل مشروع).

### د. اختبارات المنصة نفسها
- صفر اختبارات = كل نشر مخاطرة. الحد الأدنى: اختبارات وحدة لـ `turbo.ts`/`composeSite`، وعقود الأدوات، ومسار `chat.ts`.

### هـ. البنية التحتية للنطاقات والتخزين
- Wildcard DNS + شهادة wildcard لمعاينات `*.preview.domain` (Replit يعطي رابطاً فورياً لكل مشروع).
- تخزين كائنات (S3-compatible) للأصول والنسخ الاحتياطية بدل قرص الخادم.

### و. نماذج بجودة بصرية أعلى
- DeepSeek ممتاز للتكلفة، لكن الفارق البصري الحقيقي يظهر مع Claude/GPT في مرحلة «الاتجاه البصري + نقد التصميم» فقط (وليس كل الجولات).

---

## 3) ما الذي يجب إدخاله فعلياً — وهل له اشتراكات وكم تكلفته

الأسعار تقديرية شهرية بالدولار (أسعار قوائم 2026، قابلة للتغيّر).

| # | العنصر | لماذا | الحل المدفوع | التكلفة | البديل المجاني/الرخيص |
|---|---|---|---|---|---|
| 1 | **عزل تنفيذ آمن** | حماية المنصة من كود العملاء | E2B Sandboxes / Daytona / Modal | ~$20–150 حسب الاستخدام | **gVisor أو Kata Containers على Contabo الحالي = مجاني** (إعداد فقط)، أو خادم ثاني رخيص $6–12 كـ«منطقة تنفيذ» منفصلة |
| 2 | **ذاكرة دلالية** | جودة وسرعة إعادة الاستخدام | Pinecone / Weaviate Cloud | $25–70 | **pgvector على Postgres الحالي = مجاني**؛ Embeddings عبر `bge-m3`/`nomic-embed` محلياً مجاناً، أو OpenAI embeddings ~$0.02/مليون توكن |
| 3 | **نماذج عالية الجودة للتصميم** | القفزة البصرية | Claude Sonnet / GPT-5.x عبر OpenRouter | استهلاكي: ~$3–15 لكل مليون توكن؛ عملياً **$0.3–1.5 لكل مشروع** | DeepSeek V3.1 / Qwen3-Coder / Kimi K2 — تكلفة ~1/10، جودة منطقية ممتازة لكن أقل ذوقاً بصرياً. **الحل الأمثل: هجين** — نموذج رخيص للتنفيذ + نموذج غالٍ لجولة «الاتجاه البصري + النقد» فقط |
| 4 | **تخزين كائنات** | أصول، نسخ احتياطية، ZIP | Cloudflare R2 | **$0 خروج**، ~$0.015/GB (أول 10GB مجاناً) | Backblaze B2 (10GB مجاناً) أو MinIO على الخادم = مجاني |
| 5 | **Wildcard DNS + SSL للمعاينات** | رابط فوري لكل مشروع | Cloudflare | **مجاني** | Let's Encrypt wildcard عبر DNS-01 = مجاني (موجود لديك Certbot) |
| 6 | **مراقبة وأخطاء** | كشف الأعطال قبل العميل | Sentry Team $26 / Better Stack $25 | $25–30 | **Sentry مجاني 5k أحداث/شهر**، أو Uptime Kuma + Grafana/Loki ذاتي الاستضافة = مجاني |
| 7 | **CI/CD واختبارات** | منع نشر مكسور | GitHub Actions | **مجاني** للمستودعات العامة / 2000 دقيقة مجاناً للخاصة | نفسه — مجاني عملياً |
| 8 | **اختبار بصري تراجعي** | ثبات الجودة | Chromatic $149 / Percy $99 | $99–149 | **Playwright `toHaveScreenshot()` = مجاني** ويكفي تماماً لحالتك |
| 9 | **بحث ويب للوكيل** | معلومات محدثة | Exa $49 / Tavily $30 | $30–50 | Brave Search API (2000 استعلام/شهر مجاناً) أو SearXNG ذاتي = مجاني |
| 10 | **بريد معاملات** | تسليم/إشعارات العملاء | Resend $20 | $20 | Resend مجاني 3000/شهر — كافٍ |
| 11 | **خادم ثانٍ للتنفيذ** | فصل المنصة عن كود العملاء | Contabo/Hetzner | $6–15 | تأجيله ممكن بفضل نظام حوكمة الموارد الحالي |

### إجمالي التكلفة الواقعية
- **مسار «صفر اشتراكات»**: $0/شهر إضافية فوق خادم Contabo الحالي + استهلاك النماذج (~$10–40/شهر لاستخدام كثيف مع DeepSeek).
- **مسار «احترافي متوازن»**: ~$35–60/شهر (خادم تنفيذ ثانٍ $10 + Sentry $26 مؤجّل + نماذج هجينة) — يغطي 95% من فجوة Replit.
- **مسار «مؤسسي كامل»**: $250–400/شهر (E2B + Pinecone + Chromatic + Sentry) — **لا أنصح به**؛ البدائل المجانية أعلاه تعطي نفس النتيجة عملياً لمستخدم واحد.

**الخلاصة المهمة**: تقريباً كل فجوة بينك وبين Replit/Manus لها **بديل مجاني بنفس الجودة** لأن حجم استخدامك (مستخدم واحد + مشاريع عملاء) لا يحتاج طبقة SaaS مُدارة. الاشتراك الوحيد غير القابل للتجنّب هو **استهلاك نماذج الذكاء**.

---

## 4) خطة التنفيذ المقترحة (بالترتيب)

1. **pgvector + استدعاء دلالي** — أعلى أثر على الجودة، تكلفة صفر.
2. **اختبارات Vitest للأدوات + Playwright visual baselines** — تكلفة صفر، توقف الانحدارات.
3. **gVisor/Kata لعزل حاويات المشاريع** — تكلفة صفر، يغلق أخطر ثغرة.
4. **نموذج هجين**: DeepSeek للتنفيذ + Claude/GPT لجولة الاتجاه البصري والنقد فقط.
5. **Wildcard DNS للمعاينات + R2 للأصول** — تكلفة شبه صفرية.
6. **Sentry (الخطة المجانية) + لوحة مقاييس** فوق `/status` الحالية.

---

## 5) ما هو جاهز ويعمل بالفعل (لا تعِد بناءه)
مسار Turbo Build، بوابات الجودة (`auto_repair` → `run_checks` → `browser_check ≥85` → `design_review` → `seo_kit` → `publish`)، Staging مع اختبارات دخان و diff قبل الإنتاج، Rollback آمن، مراقبة `/status` و`/trace`، الإصلاح الذاتي عبر GitHub API، حوكمة الموارد و swap، النسخ الاحتياطي اليومي، تسليم المشاريع (ZIP + إنشاء repo + dump قاعدة بيانات)، النطاقات المخصّصة عبر Nginx/Certbot.
